# JP Patches lending relay — Cloudflare Worker config.
# Deploy steps in relay/README.md.
name = "jp-patches-lend"
main = "worker.js"
compatibility_date = "2026-06-01"

# Optional: serve at lend.jx-3p.com instead of the *.workers.dev URL.
# Requires jx-3p.com's DNS to be on this Cloudflare account. Uncomment
# after the first deploy, then `npx wrangler deploy` again.
# routes = [
#   { pattern = "lend.jx-3p.com", custom_domain = true }
# ]
