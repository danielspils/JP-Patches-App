# v0.8.9 — Windows For Real!

**Windows:** the installer is now code-signed — I'm paying $10/mo to be a certified Windows app dev. If they only knew I've never written a line of code (thank you, Claude!)

Windows will show "Daniel Spils" as the verified publisher instead of an unknown-publisher warning. (SmartScreen may still show its notice while the new certificate builds reputation; that fades with downloads.)

**Both platforms:** when I publish a new post to the Notes blog, a small dismissible note appears in the app — click it to read, or dismiss it and it won't return until the next post.

## Install

Auto-updates from v0.8.8 on Mac. **System requirement:** macOS 12+ on Apple Silicon (arm64). Windows 10 (64-bit) or later.
